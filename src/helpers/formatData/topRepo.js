// from a list of [GithubRepos]

// if (repoList.length === 0) {
//   return 'no code repos';
// }

// if (repoList.length === 1) {
//   return repoList[0];
// }

// return 'test';
// if (repoList.length > 1) {
//   const repoListSortbyStars = repoList.reduce((a, b) => {
//     return a.stars > b.stars ? a : b;
//     // }, {});
//     console.log('sort out reduce for stars');
//     // return repoListSortbyStars[0];
//     return null;
// }
